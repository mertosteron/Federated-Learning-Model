# common

Paylaşılan Protocol Buffers şemaları, tipler ve yardımcılar.

- `fedguard.proto` — Faz 3: gRPC mesaj şemaları (GradientUpdate, RoundStart, ProofSubmission)
- Şema versiyonu sabit: `schema_hash` Faz 4 ZK devresinde public input olarak kullanılır

Bu dizin dil-agnostik şemaların tek kaynağıdır (Python + TS istemcileri buradan üretilir).
